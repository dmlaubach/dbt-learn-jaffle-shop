version: 2

models:
  - name: stg_stripe__payments
    description: "Staging table for payment data from Stripe."
    columns:
      - name: payment_id
        tests:
          - unique
          - not_null